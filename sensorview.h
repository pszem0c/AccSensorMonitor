#ifndef SENSORVIEW_H
#define SENSORVIEW_H

#include <QWidget>

class SensorView : public QWidget {
    Q_OBJECT
public:
    explicit SensorView(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // SENSORVIEW_H
